-
  name: execute a script
  hosts: all
  tasks:
    -
      name: copy file to the location
      copy:
        src: test.sh
        dest: /home/osboxes
        mode: 0755
      delegate_to: localhost
      run_once: yes
    -
      name: execute the script
      script: /home/osboxes/test.sh
      delegate_to: localhost
      
      
      
        
      
        
